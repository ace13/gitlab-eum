---
image: node:latest

cache:
  paths:
    - node_modules/

build:
  stage: test
  script:
    - npm install
    - mkdir -p public/js public/css
    - echo "TODO: Tests"

package:
  stage: deploy
  script:
    - npm install
    - npm run build
    - npm uninstall --save-prod
    - npm pack
  artifacts:
    paths:
      - './*.tgz'
